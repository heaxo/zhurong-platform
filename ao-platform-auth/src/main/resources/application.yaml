server:
  port: 9100
  address: 0.0.0.0

spring:
  profiles:
    active: dev
  application:
    name: ao-platform-auth

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  mybatis-plus:
    mapper-locations: classpath*:/mapper/**/*.xml
    type-aliases-package: com.ao.platform.auth.entity
    configuration:
      map-underscore-to-camel-case: true

  cloud:
    nacos:
      discovery:
        server-addr: lantekha:8848
      config:
        server-addr: lantekha:8848
        file-extension: yaml
        group: DEFAULT_GROUP
  config:
    import:
      - nacos:ao-platform-auth
      - nacos:ao-platform-auth-${spring.profiles.active}
