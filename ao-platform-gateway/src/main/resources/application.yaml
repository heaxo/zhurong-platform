server:
  port: 10000

spring:
  application:
    name: ao-platform-gateway

  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
        group: DEFAULT_GROUP
  config:
    import: nacos:ao-platform-gateway
