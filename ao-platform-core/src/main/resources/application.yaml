server:
  port: 9200

spring:
  profiles:
    active: dev
  application:
    name: ao-platform-core
  cloud:
    nacos:
      username: nacos
      password: nacos
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
        group: DEFAULT_GROUP
  config:
    import: nacos:ao-platform-core
